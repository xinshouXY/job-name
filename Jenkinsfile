pipeline {
  agent {
    node {
      label 'node_no'
    }

  }
  stages {
    stage('test') {
      steps {
        git(url: 'https://github.com/xinshouXY/-.git', branch: 'master')
      }
    }
  }
}